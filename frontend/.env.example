# Environment variables template
# Copy this file to .env.development for local development

# Backend API URL (required)
# For local development, use: http://localhost:8000
# For production, use your Azure Container Apps URL
VITE_API_BASE_URL=http://localhost:8000

# Debug mode (optional)
# Set to true for detailed error messages and logging
VITE_DEBUG=true

# Azure AD Configuration (Optional)
# Uncomment these to enable Microsoft account authentication for data generation
# If not configured, the app will allow unauthenticated data generation (dev mode)
#
# To set up Azure AD:
# 1. Register an app in Azure Portal > Azure Active Directory > App Registrations
# 2. Add redirect URI: http://localhost:5173 (development) or your production URL
# 3. Copy the Application (client) ID and Directory (tenant) ID below
# 4. Enable "ID tokens" under Authentication > Implicit grant and hybrid flows
#
# VITE_AZURE_AD_CLIENT_ID=your-application-client-id-here
# VITE_AZURE_AD_TENANT_ID=your-directory-tenant-id-here (or use "common" for any Microsoft account)
